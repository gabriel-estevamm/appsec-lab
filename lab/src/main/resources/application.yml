spring:
  datasource:
    url: jdbc:mysql://mysql:3306/api_db?connectTimeout=30000&socketTimeout=60000&allowPublicKeyRetrieval=true&useSSL=false
    username: appsec
    password: appsec123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    url: jdbc:mysql://mysql:3306/api_db?connectTimeout=30000&socketTimeout=60000&allowPublicKeyRetrieval=true&useSSL=false
    user: appsec
    password: appsec123

  jwt:
    # secret: ${JWT_SECRET}
    secret: mysecret123
    expiration: ${JWT_EXPIRATION}
